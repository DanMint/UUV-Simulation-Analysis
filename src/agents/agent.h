#ifndef AGENT_H
#define AGENT_H

// ─────────────────────────────────────────────────────────────────────
// Convenience header — pulls in all four agent types.
// Prefer including the specific agent header where you only need one.
// ─────────────────────────────────────────────────────────────────────

#include "targetAgent.h"
#include "detectorAgent.h"
#include "interceptorAgent.h"
#include "seekerAgent.h"

#include "patrolAgent.h" //Chris note: need to add my new given agent header file 

